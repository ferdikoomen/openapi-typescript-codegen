{{>header}}

export type HookConfig = {
	path: string;
	method: string;
}

export type HookOptions = ConstructorParameters<typeof Request>[1]

export type HookResult<Data> = [Data | undefined, { version?: number; isLoading: boolean }]

export interface HookFactory<Input, Output> {
	(config: HookConfig): (input: Input, options: HookOptions) => HookResult<Output>
}
