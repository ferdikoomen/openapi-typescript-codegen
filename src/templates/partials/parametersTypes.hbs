{{#if parameters}}
{{#if @root.useOptions~}}
export type {{{capitalize name}}}{{{capitalize (lowercase method)}}}Params = {
  {{#each parameters}}
  {{#if description}}
  /** {{{description}}} **/
  {{/if}}
  {{{name}}}{{>isRequired}}: {{>type}},
  {{/each}}
}

{{/if}}
{{/if}}