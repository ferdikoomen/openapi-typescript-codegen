{
    type: '{{export}}',
    contains: [{{#each properties}}{{>schema}}, {{/each}}]
{{#if isReadOnly}}
    isReadOnly: {{{isReadOnly}}},
{{/if}}
{{#if isRequired}}
    isRequired: {{{isRequired}}},
{{/if}}
{{#if isNullable}}
    isNullable: {{{isNullable}}},
{{/if}}
}
