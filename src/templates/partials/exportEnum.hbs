{{#if description}}
/**
 * {{{description}}}
 */
{{/if}}
export enum {{{name}}} {
    {{#each enum}}
    {{#if description}}
    /**
     * {{{description}}}
     */
    {{/if}}
    {{{name}}} = {{{value}}},
    {{/each}}
}
