{{~#equals parent name~}}
yup.mixed().oneOf([
    {{#each enum}}
    {{{../parent}}}.{{{name}}}{{#unless @last}},{{/unless}}
    {{/each}}
])
{{~else~}}
yup.mixed().oneOf([
    {{#each enum}}
    {{{value}}}{{#unless @last}},{{/unless}}
    {{/each}}
])
{{~/equals~}}
