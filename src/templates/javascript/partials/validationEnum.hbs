yup.mixed().oneOf([
    {{#each enum}}
    {{#equals ../name name}}
    {{{value}}}{{#unless @last}},{{/unless}}
    {{else}}
    {{{../name}}}.{{{name}}}{{#unless @last}},{{/unless}}
    {{/equals}}
    {{/each}}
])
