{{~#equals parent name~}}
yup.mixed().oneOf([
    {{#each enum}}
    {{{../parent}}}.{{{name}}}{{>comma}}
    {{/each}}
]){{>validationBasics}}
{{~else~}}
yup.mixed().oneOf([
    {{#each enum}}
    {{{value}}}{{>comma}}
    {{/each}}
]){{>validationBasics}}
{{~/equals~}}
