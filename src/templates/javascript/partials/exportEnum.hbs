{{#if description}}
/**
 * {{{description}}}
 */
{{/if}}
export let {{{name}}};
(function ({{{name}}}) {

    {{#if enum}}
    {{#each enum}}
    {{{../name}}}.{{{name}}} = {{{value}}};
    {{/each}}

    {{/if}}
    {{{name}}}.definition = {{>definition}};

})({{{name}}} || ({{{name}}} = {}));
