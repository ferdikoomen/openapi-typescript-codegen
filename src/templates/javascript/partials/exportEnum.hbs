{{#if description}}
/**
 * {{{description}}}
 */
{{/if}}
export let {{{name}}};
(function ({{{name}}}) {

    {{#each enum}}
    {{{../name}}}.{{{name}}} = {{{value}}};
    {{/each}}

})({{{name}}} || ({{{name}}} = {}));
