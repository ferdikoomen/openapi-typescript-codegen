{{~#>validationBasics~}}
{{#if extends}}
{{#each extends}}
{{{this}}}.schema.concat(
{{/each}}
{{/if}}
{{~#if properties~}}
yup.object().shape({
    {{#each properties}}
    {{#equals ../parent name}}
    {{{name}}}: {{>validation parent=name}}{{>comma}}
    {{else}}
    {{{name}}}: {{>validation}}{{>comma}}
    {{/equals}}
    {{/each}}
}).noUnknown()
{{~else~}}
yup.object()
{{~/if~}}
{{#if extends}}
{{#each extends}}
)
{{/each}}
{{/if}}
{{~/validationBasics~}}
