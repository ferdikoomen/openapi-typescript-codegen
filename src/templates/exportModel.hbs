{{>header}}
{{#if imports}}

{{#each imports}}
import { {{{this}}} } from './{{{this}}}';
{{/each}}
{{/if}}

{{#equals export 'interface'}}
{{>exportInterface}}
{{else equals export 'enum'}}
{{>exportEnum}}
{{else}}
{{>exportType}}
{{/equals}}
