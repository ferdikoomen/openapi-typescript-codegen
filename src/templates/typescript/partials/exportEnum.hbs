{{#if description}}
/**
 * {{{description}}}
 */
{{/if}}
export enum {{{name}}} {
    {{#each enum}}
    {{{name}}} = {{{value}}},
    {{/each}}
}

export namespace {{{name}}} {

    export const schema: Schema<{{{name}}}> = {{>schema}};

}
