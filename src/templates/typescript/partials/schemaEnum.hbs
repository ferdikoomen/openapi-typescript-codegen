{
    type: 'enum',
    enums: [
    {{#each enum}}
        {{{value}}},
    {{/each}}
    ],
{{#if isReadOnly~}}
    isReadOnly: {{{isReadOnly}}},
{{/if}}
{{#if isRequired~}}
    isRequired: {{{isRequired}}},
{{/if}}
{{#if isNullable~}}
    isNullable: {{{isNullable}}},
{{/if}}
}
