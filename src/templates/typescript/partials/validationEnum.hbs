{{~#equals parent name~}}
yup.mixed<{{{name}}}>().oneOf([
    {{#each enum}}
    {{{../parent}}}.{{{name}}}{{#unless @last}},{{/unless}}
    {{/each}}
])
{{~else~}}
yup.mixed<{{{type}}}>().oneOf([
    {{#each enum}}
    {{{value}}}{{#unless @last}},{{/unless}}
    {{/each}}
])
{{~/equals~}}
